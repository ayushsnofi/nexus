export enum chatStatus {
    read="read",
    sent="sent",
    delivered="delivered"
}

export enum ConversationType {
    single="single",
    group="group"
}

export enum ParticipantType {
    admin="admin",
    member="member"
}